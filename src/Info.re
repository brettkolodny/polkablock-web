[@react.component]
let make = () => {
    <div id="info">
        {React.string("Enter a block hash or number to bring up information on the block or press search for the most recent block")}
    </div>
};